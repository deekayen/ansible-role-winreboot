---

galaxy_info:
  role_name: win_reboot
  author: Trond Hindenes
  description: A role to manage reboots for Windows. Check for pending reboots, then (optionally) perform a reboot on the nodes that need it.
  license: BSD
  min_ansible_version: 2.1

  platforms:
    - name: Windows
      versions:
        - all

  galaxy_tags:
    - windows
    - reboot

dependencies: []
